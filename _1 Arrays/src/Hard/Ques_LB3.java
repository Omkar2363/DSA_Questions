package Hard;

public class Ques_LB3 {

    //Ques : Median of two sorted arrays of different sizes

    //Link : https://www.geeksforgeeks.org/median-of-two-sorted-arrays-of-different-sizes/



    //Ques2 : Median of two sorted arrays of same sizes
    //Link2 : https://practice.geeksforgeeks.org/problems/find-the-median0527/1
    /*code :
        int find_median(vector<int> v)
		{
			sort(v.begin(),v.end());
			int ans ;
			// if size is odd
			if(v.size() & 1)
				ans = v[v.size() / 2];
			// If size is even
			else
				ans = (v[v.size() / 2] + v[v.size() / 2 - 1]) / 2;
			return ans;
		}
    * */

    public static void main(String[] args) {

        //Search again
    }
}
